export type SubnetSummaryFormValues = {
  active_discovery: boolean;
  allow_dns: boolean;
  allow_proxy: boolean;
  cidr: string;
  description: string;
  dns_servers: string;
  fabric: string;
  gateway_ip: string;
  managed: boolean;
  name: string;
  vlan: string;
};

summary: IPMI power driver service

description: |
  MAAS power driver for IPMI BMCs

services:
  ipmi-driver:
    override: replace
    command: $SNAP/bin/ipmi-driver start
    startup: enabled
    sockets:
      ipmi:
        listen-stream: $SNAP_COMMON/power-driver/ipmi.sock

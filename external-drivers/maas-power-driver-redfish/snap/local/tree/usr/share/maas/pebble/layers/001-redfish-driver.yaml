summary: redfish power driver service

description: |
  MAAS power driver for redfish BMCs

services:
  redfish-driver:
    override: replace
    command: $SNAP/bin/redfish-driver start
    startup: enabled
    sockets:
      redfish:
        listen-stream: $SNAP_COMMON/power-driver/redfish.sock

summary: recs power driver service

description: |
  MAAS power driver for recs BMCs

services:
  recs-driver:
    override: replace
    command: $SNAP/bin/recs-driver start
    startup: enabled
    sockets:
      recs:
        listen-stream: $SNAP_COMMON/power-driver/recs.sock

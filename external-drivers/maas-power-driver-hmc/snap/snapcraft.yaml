name: maas-power-driver-hmc
version: '1.0.0'
summary: MAAS power driver for HMC
description: MAAS power driver for HMC BMCs
grade: devel
confinement: strict
base: core26
build-base: devel

plugs:
  power-driver:
    interface: content
    content: power-driver
    target: $SNAP_COMMON/power-driver
  network-bind:

apps:
  hmc:
    command: bin/hmc-driver start
    sockets:
      hmc:
        listen-stream: $SNAP_COMMON/power-driver/hmc.sock
  status:
    command: bin/hmc-driver status

parts:
  hmc-driver:
    plugin: python
    source: .
    build-attributes:
      - enable-usrmerge
    override-build: |
      /usr/bin/python3 -m pip install --prefix=$CRAFT_PART_INSTALL/usr --no-build-isolation --root-user-action=ignore .
      find $CRAFT_PART_INSTALL -type f -executable -exec sed -i '1 s|^#!.*python3.*$|#!/usr/bin/env python3|' {} +

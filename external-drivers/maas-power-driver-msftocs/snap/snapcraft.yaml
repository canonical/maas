name: maas-power-driver-msftocs
version: '1.0.0'
summary: MAAS power driver for Microsoft OCS
description: MAAS power driver for Microsoft OCS BMCs
grade: devel
confinement: strict
base: core26
build-base: devel

plugs:
  power-driver:
    interface: content
    content: power-driver
    target: $SNAP_COMMON/power-driver

parts:
  msftocs-driver:
    plugin: python
    source: .

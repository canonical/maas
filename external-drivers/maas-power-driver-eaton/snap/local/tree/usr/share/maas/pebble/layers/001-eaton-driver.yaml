summary: eaton power driver service

description: |
  MAAS power driver for eaton BMCs

services:
  eaton-driver:
    override: replace
    command: $SNAP/bin/eaton-driver start
    startup: enabled
    sockets:
      eaton:
        listen-stream: $SNAP_COMMON/power-driver/eaton.sock

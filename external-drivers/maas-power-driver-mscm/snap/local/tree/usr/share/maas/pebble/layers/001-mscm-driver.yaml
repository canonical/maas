summary: mscm power driver service

description: |
  MAAS power driver for mscm BMCs

services:
  mscm-driver:
    override: replace
    command: $SNAP/bin/mscm-driver start
    startup: enabled
    sockets:
      mscm:
        listen-stream: $SNAP_COMMON/power-driver/mscm.sock

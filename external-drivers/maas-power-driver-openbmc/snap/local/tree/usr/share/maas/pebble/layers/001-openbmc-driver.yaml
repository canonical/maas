summary: openbmc power driver service

description: |
  MAAS power driver for openbmc BMCs

services:
  openbmc-driver:
    override: replace
    command: $SNAP/bin/openbmc-driver start
    startup: enabled
    sockets:
      openbmc:
        listen-stream: $SNAP_COMMON/power-driver/openbmc.sock

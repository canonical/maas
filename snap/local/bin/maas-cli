#!/bin/bash -e

# don't use snapcraft-preload directly as we need to pass both libraries
export LD_PRELOAD="$SNAP/lib/libsnapcraft-preload.so:$SNAP/usr/lib/stub_initgroups.so"

exec "$SNAP/bin/maas" "$@"
